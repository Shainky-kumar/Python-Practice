1. Conditionals control statements 
there are mainly 5 types of the statement in python 
    a. control statement = if,else, nested if, ladder if(elif)
    b. Looping statement =  for loop, while loop, nested for loop, nested while loop
    c. break = break
    d. continue= continue
    e. pass = pass


2. what is loop 
    user are usefull to print any statement multiple times according your conditions 

    1. for Loop
        ex: for i in range(condition)
            statement 

        (for is keyword, i is variable, in is operator, and range is inbuilt function)

        there are three valuse used in range(0,20,1)
        start value = 0
        end value = 20
        step value = 1(means 1 increment)
        agr hum step value use nhi krta to by default 1 increament lata ha 
        agr hum start value na la to vo by default 0 laga

        a. agr python ma loop forward direction ya sidha chalaga to vo n-1 tak chalaga 
        b. agr python ma loop forward direction ya sidha chalaga to vo n+1 tak chalaga 


    1. While Loop
        ex:  x = 0    => start value used alway first in while loop 
            while(condition)  => end value 
              print(x)
              x = x+1 => here is the increment 
              a.  agr ma increment value print ka bd use krta hu to mara loop starting value sa la kr end valuse tak jayaga 
              b.  agr ma increment value print ka bd use krta hu to mara loop startinf value sa jitna mana increment diea ha usko add krka start hoga to end value ma increment krna tak jayaga
              
              (check example 1 to 3 for the clerification loops.py)




